import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Pro Access - All Warzone Pro Tools | WZPRO Meta',
  description: 'Unlock WZPRO Meta Pro Tools: aim, meta, movement, spawns, mindset, PC optimization and actionable Warzone guides.',
};

export default function ProAccessLayout({ children }: { children: React.ReactNode }) {
  return children;
}
